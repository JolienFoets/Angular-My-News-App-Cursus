import { TitleFullcapsPipe } from './title-fullcaps.pipe';

describe('TitleFullcapsPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFullcapsPipe();
    expect(pipe).toBeTruthy();
  });
});
